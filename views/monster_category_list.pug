extends layout

block content
	h1= title
	
	ul
		each monster_category in monster_categories
			li 
				a(href=monster_category.url) #{monster_category.name}
		else
			li There are no monster categories.			